[pytest]
addopts = -m "not integration and not legacy"
markers =
    unit: pure Ctrl unit tests
    api: FastAPI contract tests
    db: tests using the isolated SQLite test database
    e2e: Ctrl-internal end-to-end simulation tests without real external services
    integration: integration tests that may require external services
    legacy: pre-refactor tests excluded from the safe default suite
